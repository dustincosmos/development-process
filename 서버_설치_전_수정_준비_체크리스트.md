# 서버 설치 전 수정 / 준비 체크리스트

목적:
- 이 문서는 현재 개발프로세스 프로그램을 서버에 올리기 전에 꼭 확인할 항목을 정리한 문서입니다.
- 목적은 `브라우저로 여러 사용자가 접속해도 단순하고 안전하게 운영`하는 것입니다.

기본 원칙:
- 지시 없이 실행 없음
- 저장 버튼을 눌러야만 DB 반영
- 서버에서는 개인 PC 경로를 사용하지 않음
- 업로드 원본과 산출물은 분리 보관

---

# 1. 설치 전 반드시 수정할 것

## 1-1. 절대경로 제거
현재 일부 코드에는 개인 PC 기준 절대경로가 남아 있습니다.

대표 위치:
- [/Users/jslee/Documents/개발프로세스/services/customer_report_service.py](/Users/jslee/Documents/개발프로세스/services/customer_report_service.py)
- [/Users/jslee/Documents/개발프로세스/README.md](/Users/jslee/Documents/개발프로세스/README.md)

수정 대상:
- 엑셀 템플릿 경로
- `generated` 출력 경로
- PDF 출력 경로
- 사용자 PC 전용 폰트 경로

수정 원칙:
- `db/paths.py` 또는 별도 설정 파일에서 공통 경로를 읽게 변경
- DB에는 파일 절대경로 대신 가능하면 프로젝트 루트 기준 상대경로 저장

## 1-2. 기본 관리자 비밀번호 변경
현재 기본 관리자 계정은 초기값이 있습니다.

확인 위치:
- [/Users/jslee/Documents/개발프로세스/services/shell_service.py](/Users/jslee/Documents/개발프로세스/services/shell_service.py)
- [/Users/jslee/Documents/개발프로세스/db/schema.py](/Users/jslee/Documents/개발프로세스/db/schema.py)

조치:
- 서버 배포 전 `admin` 비밀번호 즉시 변경
- 실제 사용자 계정 생성
- 사용자별 역할 부여

## 1-3. 출력 파일 템플릿 경로 외부화
고객 발송 엑셀/PDF 생성은 서버에 템플릿이 있어야 합니다.

현재 주의점:
- 템플릿 경로가 서버에 없으면 고객 출력물이 생성되지 않음

권장:
- `templates/` 폴더를 프로젝트 내부에 두고 템플릿을 보관
- 설정값으로 템플릿 경로 지정

## 1-4. 폰트/브라우저 의존성 점검
현재 출력 기능은 아래 라이브러리/환경 의존성이 있습니다.

관련:
- `openpyxl`
- `reportlab`
- `playwright`
- 서버 OS용 한글 폰트

주의:
- macOS 전용 폰트 경로는 Linux 서버에서 동작하지 않음
- Playwright는 브라우저 설치가 필요할 수 있음

조치:
- 서버 OS 기준 한글 폰트 지정
- Playwright 브라우저 설치 여부 확인

## 1-5. 서버용 실행 주소/포트 결정
현재 앱은 Streamlit 실행 구조입니다.

결정할 것:
- 접속 주소
- 포트
- 내부망만 허용할지 여부
- Reverse proxy 사용 여부

권장:
- 내부망 전용이면 사내망 IP + 방화벽 제한
- 외부 접속이면 Reverse proxy + 인증 필수

---

# 2. 설치 전 준비할 것

## 2-1. 폴더 준비
서버에 아래 폴더가 있어야 합니다.

- `uploads/`
- `generated/`
- `backups/`

설명:
- `uploads`: 사용자가 올린 원본 파일
- `generated`: 프로그램이 만든 산출물
- `backups`: DB 자동 백업

권장:
- 앱 시작 전 폴더 생성
- 쓰기 권한 확인

## 2-2. DB 백업 정책
현재 코드에는 일별 DB 백업이 들어가 있습니다.

현재 상태:
- SQLite `WAL`
- `busy_timeout`
- 일별 백업 생성

확인 파일:
- [/Users/jslee/Documents/개발프로세스/db/runtime.py](/Users/jslee/Documents/개발프로세스/db/runtime.py)
- [/Users/jslee/Documents/개발프로세스/db/schema.py](/Users/jslee/Documents/개발프로세스/db/schema.py)

추가 준비:
- `backups/` 폴더를 외부 백업 시스템에도 포함
- DB 파일뿐 아니라 `uploads/`, `generated/`도 함께 백업

## 2-3. 사용자 계정 준비
운영 전 사용자 계정을 만들어야 합니다.

필요 사용자:
- 관리자
- 개발 담당
- 물류 담당
- 조회 전용 사용자

확인 메뉴:
- `시스템관리 > 사용자관리`

## 2-4. 서버 패키지 준비
필요 Python 패키지가 설치되어 있어야 합니다.

대표:
- `streamlit`
- `pandas`
- `openpyxl`
- `reportlab`
- `playwright`

추가:
- 서버 OS용 한글 폰트

## 2-5. 브라우저 접속 테스트 준비
사용자는 브라우저로 접속합니다.

테스트 대상:
- 동시 로그인
- 저장 버튼
- 업로드
- PDF/엑셀 생성
- WMS 저장

---

# 3. 현재 코드 기준 이미 반영된 것

아래는 이미 코드에 들어가 있습니다.

- SQLite `WAL` 적용
- `busy_timeout` 적용
- 일별 DB 백업 생성
- `backups/` 폴더 사용
- 로그인/권한 구조 존재
- WMS 예약 재고 구조 존재
- 요구/지시/실험/품질/최종 단계 날짜 자동기록

즉 서버 전에는 새 기능보다 운영 준비가 우선입니다.

---

# 4. 서버 설치 후 바로 확인할 것

## 4-1. 로그인 확인
- 관리자 로그인 가능
- 일반 사용자 로그인 가능
- 권한별 메뉴 노출 확인

## 4-2. 파일 저장 확인
- 도면 업로드
- 원화 업로드
- 생성된 PDF/엑셀 저장

## 4-3. 데이터 저장 확인
- 요구 저장
- 지시 저장
- 실험 저장
- WMS 출고/입고 저장

## 4-4. 다중 사용자 확인
- 두 명 이상 동시 저장 시 오류 없는지 확인
- 재고예약이 중복 계산되지 않는지 확인

## 4-5. 백업 확인
- `backups/`에 백업 생성 확인
- DB 외 `uploads/`, `generated/`도 백업 대상인지 확인

---

# 5. 가장 우선인 배포 전 작업

순서대로 하면 됩니다.

1. 절대경로 제거
2. 관리자 비밀번호 변경
3. 템플릿/폰트 경로 서버용으로 수정
4. `uploads`, `generated`, `backups` 폴더 준비
5. 필요한 Python 패키지와 Playwright/폰트 설치
6. 내부망 접속 테스트
7. 실제 사용자 계정 생성

---

# 6. 권장 운영 방침

- 개발 데이터는 먼저 내부망에서 시험 운영
- 운영 초기에는 DB와 첨부파일을 매일 백업
- 사용자별 권한을 분리
- 물류는 WMS 메뉴에서만 출고/입고 실행
- 실험은 반드시 지시 기준으로만 실행

한 줄 정리:
- 서버 배포 전에는 기능 추가보다 `경로`, `계정`, `백업`, `폴더`, `출력 의존성` 정리가 우선입니다.
